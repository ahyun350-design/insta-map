# V-7-0 spike only — do NOT commit real keys
# Copy to ios/App/App/KakaoMapKeys.plist or Xcode build setting

# Native app key from Kakao Developers → Platform → Native app key
# NOT the same as NEXT_PUBLIC_KAKAO_MAP_KEY (JavaScript key)
KAKAO_NATIVE_APP_KEY=your_kakao_native_app_key
